# Variables de entorno — NUNCA subir al repositorio
.env

# Entorno virtual de Poetry (si se configura dentro del proyecto)
.venv/

# Caché de Python
__pycache__/
*.pyc
*.pyo

# Distribución / empaquetado
dist/
*.egg-info/

# Índice vectorial de ChromaDB (generado automáticamente al ejecutar el agente)
chroma_db/

# Caché de modelos de HuggingFace (se descargan automáticamente la primera vez)
.cache/

